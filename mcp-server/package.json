{
  "name": "codex-sdd-loop",
  "version": "0.3.3",
  "description": "MCP server for Codex SDD Loop - spec-driven loop engineering runtime for Codex",
  "type": "module",
  "bin": {
    "codex-sdd-loop-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js",
    "test": "node --test --import tsx test/*.test.ts",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
